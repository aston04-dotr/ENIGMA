/** Single source for `auth.storageKey` — must match between `createBrowserClient` and `createServerClient` (@supabase/ssr). */
export const ENIGMA_SUPABASE_AUTH_STORAGE_KEY = "enigma.supabase.auth.v1";
